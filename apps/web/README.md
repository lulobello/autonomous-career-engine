# Web application

This directory will contain the future TypeScript interface for job review, fit explanations, material approval, application state, and outcome analysis. It will consume documented API contracts and will not reproduce domain policy in UI code.

The web framework will be selected when an implemented workflow needs a user interface. No production web application exists in the foundation stage.
