# Core package

Owns canonical candidate and job models, evidence references, workflow policy, application states, and orchestration interfaces. It remains independent of specific providers, databases, renderers, and user-interface frameworks.
