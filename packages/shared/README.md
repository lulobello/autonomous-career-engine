# Shared package

Owns narrowly reusable identifiers, timestamps, result types, configuration contracts, and redaction helpers. Domain behavior stays in its owning package; this directory must not become a miscellaneous utility collection.
